# coding: utf-8

"""
    otari

    Otari, an OpenAI-compatible LLM gateway with API key management

    The version of the OpenAPI document: 0.0.0-dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CheckVerdictRequest(BaseModel):
    """
    One check_passed gate's verdict, as the caller's own verifier run produced it.  Mirrors ``JudgeVerdictRequest`` field-for-field: ``gate_id`` echoes back the gate the policy itself named (same bound, same reason), ``outcome`` is the caller's own report of the verifier's exit code (0/1/anything else, mapped by the caller to pass/fail/error), and ``detail`` is the verifier's captured stdout, capped the same way ``reasoning`` is.
    """ # noqa: E501
    detail: Optional[Annotated[str, Field(strict=True, max_length=4096)]] = ''
    gate_id: Annotated[str, Field(min_length=1, strict=True, max_length=200)]
    outcome: StrictStr
    __properties: ClassVar[List[str]] = ["detail", "gate_id", "outcome"]

    @field_validator('outcome')
    def outcome_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['pass', 'fail', 'error']):
            raise ValueError("must be one of enum values ('pass', 'fail', 'error')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CheckVerdictRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CheckVerdictRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "detail": obj.get("detail") if obj.get("detail") is not None else '',
            "gate_id": obj.get("gate_id"),
            "outcome": obj.get("outcome")
        })
        return _obj



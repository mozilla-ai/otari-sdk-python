# coding: utf-8

"""
    otari

    Otari, an OpenAI-compatible LLM gateway with API key management

    The version of the OpenAPI document: 0.12.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class OrganizationGuardrailDefinitionPublic(BaseModel):
    """
    The API-facing shape. Carries the names of the stored secrets and none of their values.
    """ # noqa: E501
    build_state: StrictStr = Field(description="Whether the worker that answered this request holds a guardrail built from this version of the definition. built means it does and the check runs; failed means that worker tried these exact arguments and could not build them, so every mandate pointing here is unevaluable; pending means it holds nothing for this version yet, which is the answer right after a write and on any worker that has not caught up within the refresh interval; disabled means the definition is switched off and nothing is built on purpose. It answers for one worker, so two reads can disagree while a write propagates. Why a build failed is never reported here: the reason is in the gateway's log")
    create_kwargs: Dict[str, Any] = Field(description="The constructor arguments the catalog does not mark secret, as they were stored. Returned in clear: the secrets were taken out of this map by flag, and a form has to round-trip an endpoint or a project id")
    create_secrets: Dict[str, StrictStr] = Field(description="The secret constructor arguments this definition holds, each as ***. Sending one back unchanged keeps the stored value; sending a new one rotates it, and leaving one out clears it")
    created_at: StrictStr
    enabled: StrictBool
    guardrail_name: StrictStr
    id: UUID
    name: StrictStr
    organization_id: UUID
    secrets_decryptable: StrictBool = Field(description="False when the stored secrets cannot be read with the current OTARI_SECRET_KEY, in which case create_secrets is empty and the definition needs its credentials sent again")
    updated_at: StrictStr
    __properties: ClassVar[List[str]] = ["build_state", "create_kwargs", "create_secrets", "created_at", "enabled", "guardrail_name", "id", "name", "organization_id", "secrets_decryptable", "updated_at"]

    @field_validator('build_state')
    def build_state_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['built', 'failed', 'pending', 'disabled']):
            raise ValueError("must be one of enum values ('built', 'failed', 'pending', 'disabled')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of OrganizationGuardrailDefinitionPublic from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of OrganizationGuardrailDefinitionPublic from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "build_state": obj.get("build_state"),
            "create_kwargs": obj.get("create_kwargs"),
            "create_secrets": obj.get("create_secrets"),
            "created_at": obj.get("created_at"),
            "enabled": obj.get("enabled"),
            "guardrail_name": obj.get("guardrail_name"),
            "id": obj.get("id"),
            "name": obj.get("name"),
            "organization_id": obj.get("organization_id"),
            "secrets_decryptable": obj.get("secrets_decryptable"),
            "updated_at": obj.get("updated_at")
        })
        return _obj



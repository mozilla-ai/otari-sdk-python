# coding: utf-8

"""
    otari

    Otari, an OpenAI-compatible LLM gateway with API key management

    The version of the OpenAPI document: 0.0.0-dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class MailSettings(BaseModel):
    """
    What this deployment can send, and what stands in the way if it cannot.
    """ # noqa: E501
    enabled: StrictBool = Field(description="Whether a transport is configured at all.")
    from_email: Optional[StrictStr] = Field(description="The 'From' address on outgoing mail, if one is configured.")
    from_name: StrictStr = Field(description="The 'From' display name on outgoing mail.")
    missing: List[StrictStr] = Field(description="Settings that must be set before mail works, in config order. Empty exactly when 'ready' is true, so the dashboard can name what to configure rather than only reporting that mail is off.")
    public_base_url: Optional[StrictStr] = Field(description="This deployment's own externally-reachable URL, used to build links in outgoing mail.")
    ready: StrictBool = Field(description="Whether a message carrying a link back to this deployment can be sent, which is what every message the control plane sends needs. Matches 'mail_ready' on /v1/bootstrap.")
    transport: StrictStr = Field(description="The transport a send would use: 'smtp', 'console' (logged, not delivered), or 'none'.")
    __properties: ClassVar[List[str]] = ["enabled", "from_email", "from_name", "missing", "public_base_url", "ready", "transport"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MailSettings from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if from_email (nullable) is None
        # and model_fields_set contains the field
        if self.from_email is None and "from_email" in self.model_fields_set:
            _dict['from_email'] = None

        # set to None if public_base_url (nullable) is None
        # and model_fields_set contains the field
        if self.public_base_url is None and "public_base_url" in self.model_fields_set:
            _dict['public_base_url'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MailSettings from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "enabled": obj.get("enabled"),
            "from_email": obj.get("from_email"),
            "from_name": obj.get("from_name"),
            "missing": obj.get("missing"),
            "public_base_url": obj.get("public_base_url"),
            "ready": obj.get("ready"),
            "transport": obj.get("transport")
        })
        return _obj



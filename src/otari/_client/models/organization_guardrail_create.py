# coding: utf-8

"""
    otari

    Otari, an OpenAI-compatible LLM gateway with API key management

    The version of the OpenAPI document: 0.0.0-dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class OrganizationGuardrailCreate(BaseModel):
    """
    Request body for mandating a guardrail across an organization.  ``credential`` is never stored as sent: it is encrypted with ``OTARI_SECRET_KEY`` and only the ciphertext is kept, the same convention `entities.WorkspaceMcpServer` and `entities.ProviderCredential` use. It is sent to the endpoint as ``Authorization: Bearer`` when the guardrail runs, so it authenticates this gateway to the guardrails service the entry names. A guardrail *vendor's* own key is not this: the guardrails service builds its guardrails from the operator's YAML and holds those itself.  A credential therefore requires ``url``. The deployment's ``guardrails_url`` is not necessarily encrypted (the shipped compose file makes it a same-host ``http://`` sidecar) and this row cannot see what it is set to, so an entry that fell back to it could not promise the bearer was sent over https. See `_require_url_for_credential`.  ``on`` is not offered. This plane mandates input-direction checks, which is the only direction the request path enforces (`services.guardrails.run_input_guardrails`); an organization that could store an output-direction mandate would be storing one nothing runs.
    """ # noqa: E501
    applies_to_all_workspaces: Optional[StrictBool] = Field(default=False, description="True runs this in every workspace of the organization, including one created later; false runs it only in the workspaces named by workspace_ids")
    credential: Optional[Annotated[str, Field(strict=True, max_length=8192)]] = Field(default=None, description="Bearer credential for this entry's endpoint. Requires url to be set, and https: an entry with no endpoint of its own falls back to the deployment's guardrails_url, which is commonly a same-host http sidecar. Encrypted at rest, never returned")
    enabled: Optional[StrictBool] = Field(default=True, description="False stops the guardrail everywhere without discarding it")
    mode: Optional[StrictStr] = Field(default='monitor', description="block rejects a flagged request with 403; monitor annotates the response and forwards it")
    on_unavailable: Optional[StrictStr] = Field(default='block', description="What a block-mode entry does when the guardrails service cannot be reached at all")
    profile: Annotated[str, Field(min_length=1, strict=True, max_length=128)] = Field(description="Profile name configured on the guardrails service, unique within the organization")
    url: Optional[Annotated[str, Field(strict=True, max_length=2048)]] = Field(default=None, description="Guardrails endpoint for this entry; null uses the deployment's guardrails_url")
    validate_kwargs: Optional[Dict[str, Any]] = Field(default=None, description="Provider-native request fields used as defaults (e.g. exa's 'type', searxng's 'engines').")
    workspace_ids: Optional[Annotated[List[UUID], Field(max_length=500)]] = Field(default=None, description="Workspaces this guardrail runs in. Must be empty when applies_to_all_workspaces is true")
    __properties: ClassVar[List[str]] = ["applies_to_all_workspaces", "credential", "enabled", "mode", "on_unavailable", "profile", "url", "validate_kwargs", "workspace_ids"]

    @field_validator('mode')
    def mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['block', 'monitor']):
            raise ValueError("must be one of enum values ('block', 'monitor')")
        return value

    @field_validator('on_unavailable')
    def on_unavailable_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['block', 'monitor']):
            raise ValueError("must be one of enum values ('block', 'monitor')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of OrganizationGuardrailCreate from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if credential (nullable) is None
        # and model_fields_set contains the field
        if self.credential is None and "credential" in self.model_fields_set:
            _dict['credential'] = None

        # set to None if url (nullable) is None
        # and model_fields_set contains the field
        if self.url is None and "url" in self.model_fields_set:
            _dict['url'] = None

        # set to None if validate_kwargs (nullable) is None
        # and model_fields_set contains the field
        if self.validate_kwargs is None and "validate_kwargs" in self.model_fields_set:
            _dict['validate_kwargs'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of OrganizationGuardrailCreate from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "applies_to_all_workspaces": obj.get("applies_to_all_workspaces") if obj.get("applies_to_all_workspaces") is not None else False,
            "credential": obj.get("credential"),
            "enabled": obj.get("enabled") if obj.get("enabled") is not None else True,
            "mode": obj.get("mode") if obj.get("mode") is not None else 'monitor',
            "on_unavailable": obj.get("on_unavailable") if obj.get("on_unavailable") is not None else 'block',
            "profile": obj.get("profile"),
            "url": obj.get("url"),
            "validate_kwargs": obj.get("validate_kwargs"),
            "workspace_ids": obj.get("workspace_ids")
        })
        return _obj


